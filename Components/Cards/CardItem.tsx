import React, { FunctionComponent } from 'react';
import styled from 'styled-components/native';
import { CardProps } from './types';

const CardItem: FunctionComponent<CardProps> = () => {
    return()
}

export default CardItem;
